/* ============================================================
   DentPilot Admin — إعداد التشغيل
   --------------------------------------------------------------
   DEMO_MODE = true  → نسخة تجريبية تفتح مباشرة على لوحة التحكم بلا
                        تسجيل دخول، وتعمل بالكامل داخل المتصفّح (بلا
                        خادم أو قاعدة بيانات). مناسبة للتجربة والنشر
                        السريع على Vercel.

   DEMO_MODE = false → الوضع الكامل مع تسجيل الدخول والصلاحيات وقاعدة
                        البيانات (Supabase). لإعادة تفعيل الحماية لاحقاً
                        غيّر القيمة إلى false فقط (واضبط متغيّرات Supabase
                        و AUTH_SECRET كما في README).

   ملاحظة: تعطيل الدخول لم يحذف أي ملف. نظام المصادقة و /api كلّها
   موجودة كما هي، ويكفي هذا المفتاح لإعادة تشغيلها.
   ============================================================ */
window.DP_CONFIG = {
  DEMO_MODE: true
};
