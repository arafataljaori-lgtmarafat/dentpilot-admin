# تقرير الاختبارات — DentPilot Admin

آلية الاختبار: تُشغَّل دوال `/api` الحقيقية على قاعدة بيانات ملفّية معزولة، دون شبكة،
مع التحقّق من مطابقة الأكواد المولّدة لخوارزمية التطبيقات الأصلية.

```bash
node test/run-tests.js
```

## النتيجة: ✅ 28 تحقّقاً ناجحاً، 0 فاشل

| # | الاختبار المطلوب | الحالة |
|---|---|---|
| 1 | إنشاء Super Admin | ✅ |
| 2 | تسجيل دخول Super Admin | ✅ |
| 3 | منع إنشاء مالك ثانٍ / إخفاء صفحة الإعداد بعد الإنشاء | ✅ |
| 4 | إنشاء وكيل | ✅ |
| 5 | دخول الوكيل | ✅ |
| 6 | منع الوكيل من الوصول للوحة المالك (الوكلاء + الإحصاءات → 403) | ✅ |
| 7 | إدخال Device ID صحيح واكتشاف التطبيق تلقائياً | ✅ |
| 8 | إنشاء كود Student | ✅ |
| 9 | التأكد أن كود Student يعمل داخل التطبيق (مطابق لـ `licenseForStudent` + يجتاز `licenseValid`) | ✅ |
| 10 | إنشاء كود Clinic | ✅ |
| 11 | التأكد أن كود Clinic يعمل داخل التطبيق (مطابق لـ `licenseForClinic`) | ✅ |
| 12 | تطبيق حدود الأكواد (تجاوز الحد اليومي → 403 برسالة الإدارة) | ✅ |
| 13 | حفظ البيانات بعد إعادة الفتح (تُقرأ من `.data/db.json`) | ✅ |
| 14 | عمل التطبيق كـ PWA | ✅ (يدوي — انظر أدناه) |

## تحقّقات إضافية مُنفَّذة

- رفض كلمة مرور خاطئة (401) ورمز جلسة غير صالح (401).
- منع توليد كود عندما لا يطابق التطبيق نوع الجهاز (Student vs Clinic).
- اختلاف ناتج خوارزميتَي Student و Clinic لنفس المعرّف (تأكيد فصل النظامين).
- عرض الجلسة للحد والاستهلاك والمتبقّي للوكيل بدقّة.
- رفع المالك للحد اليومي يسمح بالتوليد من جديد.
- إيقاف الوكيل يمنع دخوله فوراً (403).
- بناء قائمة «التفعيلات» من الأجهزة المميّزة المشتقّة من سجلّ الأكواد.
- صحّة بطاقات لوحة المعلومات (عدد الوكلاء/الأكواد/التفعيلات/أكواد اليوم).

## اختبار PWA (يدوي)

1. `node server.js` ثم افتح `http://localhost:3000` (أو رابط Vercel عبر HTTPS).
2. تحقّق من تسجيل Service Worker (DevTools → Application → Service Workers).
3. «تثبيت التطبيق» من شريط المتصفّح أو «إضافة إلى الشاشة الرئيسية» على الجوال.
4. يُقدَّم هيكل الواجهة من الكاش عند انقطاع الشبكة، بينما تبقى طلبات `/api` من الشبكة دائماً.

> ملاحظة: الاختبارات الآلية تغطّي منطق الخادم والتفعيل والحدود والثبات.
> شاشات الواجهة وتدفّق PWA يُتحقَّق منها يدوياً حسب الخطوات أعلاه.

## التحقّق من مسار الإنتاج (Supabase) دون شبكة

استُخدم خادم PostgREST وهمي (in-memory، مشترك بين الطلبات) لتشغيل مسار Supabase الفعلي:

- **المسار السليم:** إنشاء المالك ← تسجيل خروج ← دخول بنفس البيانات ← وصول للوحة → **12/12 ناجح**،
  و`/api/health` يُظهر `supabase / reachable`. ثبات البيانات مثبت لأن الدخول عثر على المالك
  الذي أُنشئ في طلب HTTP سابق منفصل.
- **إخفاق أ — إنتاج بلا Supabase:** يعيد `/api/health` و`/api/bootstrap` و`/api/login` رسالة **503**
  واضحة تطلب ضبط `SUPABASE_URL` و`SUPABASE_SERVICE_KEY` (بدل الفشل الصامت السابق).
- **إخفاق ب — مفتاح anon بدل service_role:** `/api/health?deep=1` يعيد **502 read_only**
  مع توجيه لاستخدام `service_role`، وإنشاء المالك يفشل برسالة صلاحية واضحة.

### نقطة التشخيص `/api/health`
افتحها على رابط الإنتاج للتأكّد من التهيئة والاتصال بقاعدة البيانات (تفاصيل في `README.md`).
